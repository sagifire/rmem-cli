{
    "name": "rmem-cli-workspace",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "tsc -b",
        "test": "npm run build && node --test dist-tests/*.test.js",
        "check": "npm run build && npm run test"
    },
    "devDependencies": {
        "@types/node": "^22.14.0",
        "typescript": "^5.8.3"
    }
}
